---
layout: layouts/base.njk
hidePagefindSearch: true
pagination:
  data: coursesList
  size: 1
  alias: c
permalink: "/courses/{{ c.slug }}/play-script/index.html"
---
{% import "components/course-breadcrumb.njk" as breadcrumb %}
{% import "components/drive.njk" as drv %}
{% import "macros/i18n.njk" as i18nMacro %}

{# Course navigation with home pill highlighted and materials dropdown #}
{{ breadcrumb.breadcrumb(c, coursesList, 'play-script', currentLang, i18n) }}


<section class="space-y-6">
  <header data-course-i18n="{{ c.i18n | json }}">
    <h1 class="text-2xl md:text-3xl font-semibold">{{ i18n[currentLang].course.play_script }}</h1>
  </header>

  <article class="prose prose-stone max-w-none">
    {% if c.docs and c.docs.play_script and c.docs.play_script.content %}
      {% if c.docs.play_script.mimeType != 'application/vnd.google-apps.document' and not c.docs.play_script.mimeType.startsWith('text/') %}
        <div class="bg-amber-50 border border-amber-200 rounded-lg p-6">
          <h3 class="text-amber-800 font-medium mb-3">📖 {{ c.docs.play_script.name }}</h3>
          {% if c.docs.play_script.mimeType === 'application/pdf' and not c.docs.play_script.content.includes('PDF 文字提取失敗') and not c.docs.play_script.content.includes('此檔案需要下載檢視') %}
            <div class="whitespace-pre-wrap font-mono text-sm leading-relaxed bg-white border rounded-lg p-6 mb-4">{{ c.docs.play_script.content }}</div>
          {% else %}
            <div class="whitespace-pre-wrap text-amber-700 mb-4">{{ c.docs.play_script.content }}</div>
          {% endif %}
          <a href="{{ c.docs.play_script.downloadUrl }}" target="_blank" class="inline-flex items-center gap-2 bg-amber-600 text-white px-4 py-2 rounded-md hover:bg-amber-700 transition-colors">
            <span>開啟文件</span>
            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 6H6a2 2 0 00-2 2v10a2 2 0 002 2h10a2 2 0 002-2v-4M14 4h6m0 0v6m0-6L10 14"></path>
            </svg>
          </a>
        </div>
      {% else %}
        <div class="bg-amber-50 border border-amber-200 rounded-lg p-4 mb-6">
          <h3 class="text-amber-800 font-medium mb-2">📖 劇本內容</h3>
          <p class="text-amber-700 text-sm">以下內容來自 Google 文件，保持原始格式</p>
        </div>
        <div class="whitespace-pre-wrap font-mono text-sm leading-relaxed bg-white border rounded-lg p-6">{{ c.docs.play_script.content }}</div>
      {% endif %}
      <div class="mt-6 text-sm text-stone-500">
        最後更新：{{ c.docs.play_script.lastUpdated | formatDate }}
      </div>
    {% elif c.files.play_scripts and c.files.play_scripts.length > 0 %}
      <div class="bg-amber-50 border border-amber-200 rounded-lg p-4 mb-6">
        <h3 class="text-amber-800 font-medium mb-2">📖 劇本內容</h3>
        <p class="text-amber-700 text-sm">以下為劇本 PDF 檔案</p>
      </div>
      {% for pdf in c.files.play_scripts %}
        {{ drv.pdfViewer(pdf) }}
      {% endfor %}
    {% else %}
      <div class="bg-stone-100 border border-stone-200 rounded-lg p-6 text-center text-stone-600">
        <h3 class="text-lg font-medium mb-2">劇本內容尚未載入</h3>
        <p>請在課程 JSON 中設定 <code class="bg-stone-200 px-2 py-1 rounded text-sm">google_docs.play_script</code> 文件 ID</p>
        <p>或在 <code class="bg-stone-200 px-2 py-1 rounded text-sm">files.play_scripts</code> 陣列中加入 PDF 檔案 ID</p>
        <p class="mt-2">設定完成後執行：<code class="bg-stone-200 px-2 py-1 rounded text-sm">npm run sync:drive</code></p>
      </div>
    {% endif %}
  </article>
</section>