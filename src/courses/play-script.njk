---
layout: layouts/base.njk
hidePagefindSearch: true
pagination:
  data: coursesList
  size: 1
  alias: c
permalink: "/courses/{{ c.slug }}/play-script/index.html"
---
{% import "components/course-breadcrumb.njk" as breadcrumb with context %}
{% import "components/drive.njk" as drv %}
{% import "macros/i18n.njk" as i18nMacro %}

{# Course navigation with home pill highlighted and materials dropdown #}
{{ breadcrumb.breadcrumb(c, coursesList, 'play-script', currentLang, i18n) }}

{% set playScriptDoc = materialHelpers.getDoc(c, 'play_script') %}
{% set playScriptPdfs = materialHelpers.getMaterialItems(c, 'play_scripts') %}


<section class="space-y-6">
  <header data-course-i18n="{{ c.i18n | json }}">
    <h1 class="text-2xl md:text-3xl font-semibold">{{ i18n[currentLang].course.play_script }}</h1>
  </header>

  <article class="prose prose-stone max-w-none">
    {% if playScriptDoc and playScriptDoc.content %}
      {% set playMime = playScriptDoc.mimeType or '' %}
      {% if playMime != 'application/vnd.google-apps.document' and (playMime and not playMime.startsWith('text/')) %}
        <div class="bg-amber-50 border border-amber-200 rounded-lg p-6">
          <h3 class="text-amber-800 font-medium mb-3">📖 {{ playScriptDoc.name }}</h3>
          {% if playMime === 'application/pdf' and not playScriptDoc.content.includes('PDF 文字提取失敗') and not playScriptDoc.content.includes('此檔案需要下載檢視') %}
            <div class="whitespace-pre-wrap font-mono text-sm leading-relaxed bg-white border rounded-lg p-6 mb-4">{{ playScriptDoc.content }}</div>
          {% else %}
            <div class="whitespace-pre-wrap text-amber-700 mb-4">{{ playScriptDoc.content }}</div>
          {% endif %}
          <a href="{{ playScriptDoc.downloadUrl }}" target="_blank" class="inline-flex items-center gap-2 bg-amber-600 text-white px-4 py-2 rounded-md hover:bg-amber-700 transition-colors">
            <span>開啟文件</span>
            <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M10 6H6a2 2 0 00-2 2v10a2 2 0 002 2h10a2 2 0 002-2v-4M14 4h6m0 0v6m0-6L10 14"></path>
            </svg>
          </a>
        </div>
      {% else %}
        <div class="bg-amber-50 border border-amber-200 rounded-lg p-4 mb-6">
          <h3 class="text-amber-800 font-medium mb-2">📖 劇本內容</h3>
          <p class="text-amber-700 text-sm">以下內容來自 Google 文件，保持原始格式</p>
        </div>
        <div class="whitespace-pre-wrap font-mono text-sm leading-relaxed bg-white border rounded-lg p-6">{{ playScriptDoc.content }}</div>
      {% endif %}
      <div class="mt-6 text-sm text-stone-500">
        最後更新：{{ (playScriptDoc.lastSynced or playScriptDoc.lastUpdated) | formatDate }}
      </div>
    {% elif playScriptPdfs and playScriptPdfs.length > 0 %}
      {% for pdf in playScriptPdfs %}
        {{ drv.pdfViewer(pdf) }}
      {% endfor %}
    {% else %}
      <div class="bg-stone-100 border border-stone-200 rounded-lg p-6 text-center text-stone-600">
        <h3 class="text-lg font-medium mb-2">劇本內容尚未載入</h3>
        <p>請在課程 JSON 中設定 <code class="bg-stone-200 px-2 py-1 rounded text-sm">docs.play_script.id</code> 或 <code class="bg-stone-200 px-2 py-1 rounded text-sm">material.play_scripts</code> 的 Drive 資料。</p>
        <p class="mt-2">設定完成後執行：<code class="bg-stone-200 px-2 py-1 rounded text-sm">npm run sync:drive</code></p>
      </div>
    {% endif %}
  </article>
</section>
